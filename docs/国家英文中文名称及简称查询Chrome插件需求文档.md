### 一、项目背景与目标

- **背景**：在多语言文档编写、跨境电商、国际业务沟通中，经常需要快速查询国家的中英文名称及国家简称（如 ISO 3166-1 Alpha-2/Alpha-3）。频繁打开网站搜索效率低，易出错。
- **目标**：开发一个基于 Chrome 插件的“国家名称与简称查询工具”，实现浏览器内一键查询、复制和校对国家英文名、中文名和简称，提升工作效率与准确性。

### 二、产品整体描述

- **产品形态**：Chrome 浏览器扩展（Chrome Extension，Manifest V3）。
- **主要功能概述**：
  - 通过插件图标弹出页面，查询国家英文名、中文名和国家代码。
  - 支持按中文、英文、代码模糊搜索。
  - 支持一键复制国家英文名、中文名或代码。
  - 支持在网页选中文本后，右键菜单/快捷键直接查询。
- **目标用户**：
  - 外贸业务人员、跨境电商运营。
  - 翻译、文案、产品经理等需要频繁处理国家信息的岗位。
  - 任何需要标准化国家名称与代码的用户。

### 三、使用场景（Use Cases）

1. **在写英文邮件时需要确认国家英文名是否标准**
   - 用户打开邮件编辑页 → 想确认“捷克共和国”的英文写法 → 点击插件图标 → 输入“捷克” → 显示“Czech Republic / Czechia”及国家代码 → 一键复制英文名。

2. **在跨境电商后台填写目的国代码**
   - 用户需要填写目的地国家的两位代码（如 US, CN, DE）→ 打开插件 → 输入中文“德国” → 显示“Germany / 德国 / DE / DEU” → 一键复制“DE”。

3. **在网页上看到一个国家英文名，想知道中文和代码**
   - 用户在网页上选中“Netherlands” → 右键菜单选择“查询国家信息” → 弹出结果窗/插件弹窗 → 显示“荷兰 / Netherlands / NL / NLD”。

4. **快速校对 Excel/表单中的国家英文**
   - 用户在浏览器在线表格中编辑 → 不确定英文是否标准 → 复制英文名 → 在插件内搜索 → 核对标准写法和代码。

### 四、功能需求

#### 4.1 查询与搜索

- **F1-查询入口（插件弹窗）**
  - 点击 Chrome 工具栏中的插件图标，打开弹窗页面（`popup.html`）。
  - 弹窗顶部为搜索输入框。
- **F2-搜索方式**
  - 支持以下任一关键字模糊搜索：
    - 中文名（如“美国”、“阿联酋”）。
    - 英文名（如“United States”、“UAE”）。
    - 国家代码：
      - Alpha-2（如“US”、“CN”、“DE”）。
      - Alpha-3（如“USA”、“CHN”、“DEU”）。
  - 搜索时实时过滤列表（输入时动态展示匹配结果）。
- **F3-结果展示**
  - 每条结果需展示字段（可视为一行数据）：
    - 中文名：如“美国”
    - 英文名：如“United States of America”
    - Alpha-2 代码：如“US”
    - Alpha-3 代码：如“USA”
    - （可选）数字代码：如“840”
  - 支持多条结果（如“刚果”、“刚果民主共和国”、“刚果共和国”等歧义情况）列表展示。

#### 4.2 一键复制

- **F4-复制功能**
  - 在每条结果中，为以下字段提供独立的“复制”按钮或点击即复制功能：
    - 复制英文名
    - 复制中文名
    - 复制 Alpha-2 代码
    - 复制 Alpha-3 代码
  - 复制后，在插件内短暂显示“已复制 xxx”提示（Toast/气泡提示）。

#### 4.3 右键菜单与选中文本查询

- **F5-右键菜单**
  - 安装插件后，在浏览器右键菜单中增加一项：
    - 对选中文本：显示“使用国家查询插件搜索 ‘xxx’”。
  - 用户在网页上选中文字（可能为中/英文名或代码），右键点击该菜单项：
    - 打开插件弹窗并自动填入选中文本进行搜索；
    - 或在新小窗口中展示查询结果（二选一，需求需在设计阶段确定）。
- **F6-快捷键调用（可选）**
  - 支持自定义键盘快捷键（Chrome 扩展提供的快捷键机制），如：
    - 全局：`Alt + Shift + C` 打开插件弹窗。
    - 在有选中文本时：通过快捷键直接以选中内容进行查询。

#### 4.4 数据源与数据管理

- **F7-国家数据源**
  - 使用标准的国家列表数据，例如基于 ISO 3166-1。
  - 数据格式建议为本地 JSON 文件，包含字段：
    - `englishName`：英文官方名称
    - `chineseName`：中文名称
    - `alpha2`：两位国家简码
    - `alpha3`：三位国家简码
    - `numeric`：（可选）数字代码
    - `aliases`：（可选）别名、常用缩写，用于提高搜索体验。
- **F8-本地存储**
  - 所有国家数据在插件内本地存储，不依赖外部接口，保障离线可用和稳定性。
  - 插件更新时可更新 JSON 数据文件。

#### 4.5 个性化与设置（可选）

- **F9-设置页面**
  - 允许用户调整：
    - 默认复制字段（如优先复制英文名或 Alpha-2 代码）。
    - 是否显示数字代码列。
    - 是否启用右键菜单、快捷键功能。
- **F10-最近使用/收藏**
  - 记录最近查询的若干个国家（如最近 10 条），在无输入时优先展示。
  - 支持将某些国家加入“常用”列表，置顶展示。

### 五、非功能需求

#### 5.1 性能与体验

- **N1-快速响应**
  - 本地查询，搜索响应时间应基本即时（< 100ms，可感知为无延迟）。
- **N2-轻量级**
  - 插件包体适中，避免引入与功能无关的大型依赖库。
- **N3-易用性**
  - 界面简洁、布局清晰，支持中英文用户轻松理解。
  - 搜索框默认聚焦，打开弹窗即可直接输入。

#### 5.2 兼容性

- **N4-浏览器版本**
  - 支持最新稳定版 Chrome 浏览器。
  - 使用 Manifest V3 规范实现。
- **N5-分辨率与暗色模式（可选）**
  - 适配常见分辨率下的弹窗显示。
  - 优先支持浅色模式，后期可扩展暗色模式主题。

#### 5.3 安全与隐私

- **N6-权限最小化**
  - 仅申请必要权限：
    - `contextMenus`（右键菜单）
    - `activeTab`（可选，用于选中文本场景）
    - `storage`（若需要存储配置、收藏、最近使用）
  - 不访问用户隐私数据（如浏览历史、Cookie 等）。
- **N7-离线可用**
  - 不依赖服务器或网络环境，纯本地数据查询。

### 六、UI/交互设计要求（概要）

#### 6.1 弹窗主界面结构（示意）

- **顶部区域**
  - 搜索输入框：占据主宽度。
  - 占位提示文字：如“输入国家中文/英文/代码进行搜索”。
- **中部列表区域**
  - 表头（可选）：`中文名 | 英文名 | Alpha-2 | Alpha-3 | 操作`
  - 列表每行：
    - 国家中文名
    - 国家英文名
    - 两位码
    - 三位码
    - 若干“复制”按钮或图标（复制英文、复制中文、复制代码）。
- **底部区域（可选）**
  - 最近使用 / 常用国家展示区。

#### 6.2 交互细节

- 输入时实时触发搜索，按回车键等同于“搜索”。
- 鼠标悬停在复制按钮上显示 Tooltip，如“复制英文名”。
- 复制成功后，短暂显示提示（1–2 秒消失）。

### 七、技术实现要求（概要）

- **技术栈**：
  - 原生 HTML/CSS/JavaScript 或基于简单框架（如 React）实现弹窗 UI（视团队情况而定）。
- **主要文件**：
  - `manifest.json`：扩展配置（名称、版本、权限、图标、popup 页面等）。
  - `popup.html` + `popup.js`：弹窗页面和逻辑。
  - `background.js`/service worker：右键菜单、快捷键处理等。
  - `countries.json`：国家数据文件。
  - `options.html` + `options.js`：（可选）设置页面。

### 八、验收标准

- **功能验收**
  - 能通过插件图标弹出查询界面。
  - 能通过中文、英文和国家代码进行搜索并正确展示结果。
  - 一键复制功能可用，复制内容正确。
  - 右键菜单在选中文本时可用，能跳转至查询结果。
- **数据准确性**
  - 国家列表、英文名、中文名和代码与设定的数据源（如 ISO 3166-1 标准 + 内部翻译规范）一致。
- **稳定性**
  - 正常使用过程中无明显报错或崩溃。
  - 网络断开时功能不受影响。

### 九、安装与使用说明

#### 9.1 开发环境安装（加载已解压扩展）

1. 在本地确保已安装 Chrome 浏览器（推荐最新版稳定版）。  
2. 将本项目代码放置在本地任意目录，例如：`/home/dev/Downloads/ccq`。  
3. 在 Chrome 地址栏输入 `chrome://extensions/` 打开“扩展程序”管理页面。  
4. 右上角打开“开发者模式”（Developer mode）。  
5. 点击“加载已解压的扩展程序”（Load unpacked）。  
6. 在弹出的文件选择对话框中，选择项目根目录（包含 `manifest.json` 的目录，如 `ccq`），点击“选择”。  
7. 若加载成功，可在扩展列表中看到“国家英文/中文名称及简称查询”插件，并在浏览器工具栏看到对应图标。

#### 9.2 基础使用方式（弹窗查询）

1. 在任意网页中，点击浏览器工具栏上的插件图标。  
2. 会弹出插件主界面：  
   - 顶部为搜索输入框，默认自动聚焦。  
   - 中间为结果表格，展示“中文名 / 英文名 / Alpha-2 / Alpha-3 / 数字码 / 操作”。  
3. 在搜索框中输入任意关键字，即可进行模糊搜索：  
   - 输入中文名，如“美国”“德国”。  
   - 输入英文名，如“United States”“Germany”。  
   - 输入国家代码，如“US”“DE”“USA”“DEU”“840”等。  
4. 搜索结果中，每一行国家信息右侧提供多个“复制”按钮：  
   - “复制中文”：复制该国家中文名。  
   - “复制英文”：复制该国家英文名。  
   - “复制 A2”：复制 Alpha-2 代码。  
   - “复制 A3”：复制 Alpha-3 代码。  
5. 点击对应按钮后，剪贴板中会得到相应内容，同时弹窗底部会短暂显示“已复制xxx”的提示。

#### 9.3 右键菜单查询方式

1. 在网页中选中任意一段文本（支持中文、英文或代码），例如：  
   - “Netherlands”  
   - “德国”  
   - “US” / “USA”  
2. 在高亮选中状态下，点击鼠标右键，在右键菜单中选择：  
   - “使用国家查询插件搜索 ‘xxx’”（xxx 为当前选中内容）。  
3. 插件会自动将该文本作为搜索关键字，打开弹窗并展示匹配的国家信息。  
4. 后续操作与弹窗查询方式一致，可直接点击复制按钮获取对应字段。

#### 9.4 键盘快捷键打开插件（可配置）

1. 插件默认注册快捷键：`Alt + Shift + C`。  
2. 可在 Chrome 中通过以下路径修改快捷键：  
   - 打开 `chrome://extensions/shortcuts`（扩展程序快捷键设置页）。  
   - 找到“国家英文/中文名称及简称查询”扩展下的“open_country_search”命令。  
   - 点击右侧输入框，并按下希望设置的组合键进行修改。  
3. 设置完成后，在任意标签页按下配置好的快捷键，即可快速打开插件弹窗进行查询。

#### 9.5 常见问题

- **看不到插件图标？**  
  - 可能被收纳到浏览器右侧的扩展菜单中，点击拼图状扩展按钮，将本插件固定到工具栏。  
- **右键菜单未出现？**  
  - 确认插件已成功安装且开启；若仍无效，可尝试在扩展管理页面中关闭再重新开启，或卸载后重新加载。  
- **复制失败或无反应？**  
  - 某些操作系统或浏览器策略可能限制剪贴板访问。请确认浏览器未处于极端隐私/安全模式，或尝试在其他页面再次测试。  
- **国家列表不全或有误？**  
  - 后续可通过更新 `countries.json` 文件扩展和修正国家数据，更新后重新加载扩展即可生效。
